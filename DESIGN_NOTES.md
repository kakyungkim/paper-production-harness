*Designed by Ka-Kyung Kim, 2026 — a reusable paper-production harness, contributed as a scaffold.*

# 설계 노트 — 왜 이렇게 구성했나

이 스캐폴드는 논문 한 편을 분석부터 발표까지 끌고 가는 일을 **반복 가능한 하네스**로 만든 것이다. 주제는 갈아끼울 수 있게 두되, 일을 굴리는 구조(역할, 호출 순서, 산출물 약속, 검수·검증 규율)는 그대로 재사용한다. 아래는 그 구조를 왜 이렇게 잡았는지에 대한 방법론 메모다.

## Agent와 Skill을 나눈 이유

둘을 구분하는 기준은 단순하다. **Agent는 "가서 X를 해라"는 일꾼이고, Skill은 "X를 어떻게 하는가"라는 방법서다.** 분석을 돌리고, 본문을 쓰고, 검수하는 주체는 agent로 둔다. 반면 그림을 결과 파일에서 생성하는 절차나 발표 덱을 렌더하는 절차처럼 "정해진 방식"은 Skill로 둔다. 이렇게 나누면 같은 방법서를 여러 agent가 공유하고, 일꾼을 바꿔도 방법은 유지된다. 실제로 그림 생성은 agent가 아니라 Skill/스크립트로 두기를 권한다. agent로 호출하면 동작하지 않고, 결과 파일에서 결정론적으로 다시 그리는 편이 재현성에도 맞기 때문이다.

## 역할 조직도

랩을 본뜬 구성이다. 사람과 메인 루프가 PI 역할을 맡아 호출과 승인, 공개 게이트를 책임진다. 그 아래로 문헌·기획(선행연구, 차별화, 방법론), 분석실(주제별 분석과 선택적 잠정 판정), 집필실(본문, 발표), 심사·QA(적대적 자체검토, 정식 리뷰)로 벤치를 나눈다. 역할을 잘게 나누는 까닭은 한 일꾼에게 모든 맥락을 욱여넣지 않기 위해서다. 검수자는 검수만, 집필자는 집필만 맡으면 각자의 판단이 서로의 편향에 덜 오염된다.

## 산출물 계약 — 대화가 아니라 파일로 넘긴다

핵심 규율 하나는 **멤버가 결과를 대화에만 남기지 않고 정해진 파일로 넘긴다**는 것이다. 분석은 결과 JSON으로, 집필은 원고 파일로, 검수는 노트로 남긴다. 다음 단계는 앞 단계의 대화 기억이 아니라 그 파일을 읽는다. 이렇게 하면 단계가 서로 느슨하게 풀린다. 그림만 다시 그리거나 리뷰만 다시 받는 부분 재실행이 가능해지고, 한 단계가 실패해도 앞 산출물이 파일로 남아 있어 처음부터 다시 돌릴 필요가 없다. 숫자를 메모리에서 재유도하지 않고 항상 결과 파일에서 인용하게 만드는 장치이기도 하다.

## 오케스트레이터를 진행표로 둔 이유

`paper-orchestrator`(agent)는 계획만 세운다. subagent는 다시 subagent를 부를 수 없기 때문에, 실제 실행은 메인 루프가 맡는다. 그래서 실행 입구는 별도의 **Skill(`paper-production-orchestrator`)** 로 둔다. 이 Skill은 진행표 겸 팀장처럼 동작한다. 들어온 요청이 전체 파이프라인인지, 특정 부분 재실행인지, "지적을 반영해 하류만 다시"인지 먼저 분기한 뒤, 필요한 단계만 정해진 순서로 호출한다. 부분 재실행을 일급으로 다루는 것이 이 패턴의 값어치다. 매번 전 과정을 다시 돌리지 않고, 바뀐 지점의 아래 단계만 다시 흐르게 한다.

분석 단계에서 한 가지를 늘 점검한다. LLM을 쓰는 분석이 API 키 없이 **오프라인 mock 경로**로 돌았는지 확인하고, mock이면 "실제 결과가 아니라 데모"임을 보고에 명시한다. 성공처럼 보이는 실행이 사실은 빈 껍데기일 수 있어서다.

## 검수를 두 단계로 나눈 이유

리뷰를 **내부 적대적 검수 + 결정론적 검증 게이트를 먼저, 그다음 외부 referee** 순으로 둔다. 순서가 중요하다.

같은 모델을 자기 자신의 검수자로 다시 돌리는 것만으로는 자기 환각을 잡지 못한다. 그 숫자를 지어낸 바로 그 분포가 검수도 똑같이 그럴듯하다고 통과시키기 때문이다. 그래서 두 가지를 함께 둔다. 하나는 **결정론적 검증 게이트**다. 헤드라인 숫자를 결과 파일에서 코드로 다시 계산해 본문과 맞는지 기계적으로 대조한다. 모델의 판단이 아니라 재계산이라 환각이 끼어들 틈이 없다. 다른 하나는 **다른 엔진/다른 관점의 검수**다. 적대적 critic이 도표의 글자 깨짐, 잘림, 과대주장, 누락된 baseline까지 사람처럼 직접 열어 본다. 이 둘을 통과한 뒤에야 외부 referee를 부른다. referee는 사전 QA가 끝났다고 가정하고 과학적 판단만 한다.

## 공개 전 검증 게이트 규율

커밋이나 공개 전에 검증 게이트를 **반드시** 통과시킨다. 게이트가 실패하면 멈추고 사람에게 보고하며, 통과 전에는 게시하지 않는다. 공개 자체도 사람 승인 게이트 뒤로 둔다. 소속·IP·저자 확인 같은 사안은 자동화 대상이 아니라 사람이 책임질 일이기 때문이다.

## 도메인 분석 agent는 갈아끼우는 슬롯

이 스캐폴드에서 주제에 묶이는 부분은 사실상 하나, **`<DOMAIN_ANALYSIS_AGENT>`** 뿐이다. 팀은 자기 주제의 분석/실험 agent 하나만 채우면 된다. 방법론, 문헌 탐색, 집필, 검수, 리뷰, 발표, 디자인, 오케스트레이터는 주제와 무관하게 그대로 쓴다. 분석 슬롯을 명시적으로 분리해 둔 덕분에, 주제가 바뀌어도 나머지 연결 조직은 손대지 않고 재사용할 수 있다. 멤버가 일급이어도 연결 조직(입구, 라우팅, 산출물 계약, 검증 게이트)이 비면 하네스 성숙도가 낮은데, 이 스캐폴드는 그 연결 조직을 함께 제공한다.

## 검증을 더 깊게 — 게이트를 믿기 전에 게이트를 검증한다

위의 "두 단계 검수"는 재계산 게이트와 적대적 검수를 둔다. 실전에서 한 층이 더 필요했다. 게이트가 있다는 것과 게이트가 실제로 결함을 잡는다는 것은 다르기 때문이다. 아래는 그 위에 얹은 규율이고, 실행체는 `skills/verify-harness`(3층: AKM 사실검증 + mutation + 검사 카탈로그·독립성 사다리)다.

**게이트가 공허하지 않은지 먼저 본다(mutation).** 재계산 게이트가 통과해도, 그 게이트가 어떤 결함도 못 잡는 빈 검사라면 통과는 무의미하다. 실무자가 저지를 실수를 사본에 일부러 주입해 본다 — 근거 없는 숫자 삽입, 근거 파일을 갱신하고 원고를 stale로 방치, claim 등급을 슬쩍 격상, 한계 문단 삭제. 게이트가 그걸 잡으면 그 검사는 살아 있고, 못 잡으면 그 결함 부류에 대해 NOT_TESTED다. 이렇게 검증이 자기 자신도 검증한다.

**검증 리포트를 검사 대상과 같은 코퍼스에 두지 않는다.** 리포트를 원고·결과와 같은 폴더에 쓰면, 다음 번 검사가 자기가 쓴 리포트를 근거로 읽어 통과시키는 circular evidence가 생긴다. 약한 판정이 근거로 둔갑해 번지는 것이다. 리포트는 검사 대상 밖 디렉토리에 격리한다.

**재계산은 '실행됨'이 아니라 'diff 0'이다.** 게이트가 exit 0으로 끝났다는 건 실행에 성공했다는 뜻이지 값이 맞다는 뜻이 아니다. 게이트를 돌린 뒤 산출물이 커밋된 값과 byte-identical인지(git diff가 비었는지)로 판정한다. 이때 게이트가 새로 더럽힌 파일만 되돌려 다른 사람의 미커밋 작업을 건드리지 않는다.

**증거에는 독립성 사다리가 있다.** 같은 모델의 자기비평 < 도구·환경 출력 < 결정적 검사 < 다른 모델(cross-model)의 적대 검증 < 책임 있는 사람/advisor. 낮은 계단만으로 영향이 큰 수정을 승인하지 않는다. 같은 분포가 숫자도 지어내고 검수도 통과시키는 문제는 계단을 올려야 줄어든다.

**결과가 나오기 전에 표를 고정한다(사전등록).** 표 구조와 쓸 검정을 결과가 오기 전에 정하고 수치 셀은 비워 둔다. 결과를 본 뒤 유리한 지표를 고르는 사후 선택(p-hacking의 자동화판)을 막는다. 발표자료에 실린 관측치를 합격 임계로 옮겨 적지 않는다 — 임계는 봉인된 사전등록 문서의 `파일:줄`에서만 인용한다.

**실패는 실패 리포트로 낸다.** 실험–비판–수정 루프에 상한을 둔다. 상한을 넘도록 원래 목표가 계속 반박되면, 억지 성공 서사로 포장하지 말고 실패 리포트를 낸다. 음성 결과를 양성으로 만들지 않는 것이 이 하네스의 정직성 계약이다.

**headline claim은 방어가능성 게이트를 통과하기 전엔 잠정이다.** 핵심 주장은 반증기준 + make-or-break 검정 통과 + advisor 통과 전까지 PROVISIONAL로 두고 본문에 반영하지 않는다. 통계가 뒷받침하지 않는 주장을 본문에 올리지 않기 위한 앞단 잠금이다.

**검수 심도를 산출물의 위험에 맞춘다.** 모든 산출물을 같은 깊이로 볼 필요는 없다. 기계로 재생성되는 것(A), 루틴(B), 무결성·공개물(C)로 위험을 나눠 심도를 매칭한다. 판정 어휘도 `auto_pass·provisional·needs_human·blocked`로 벌린다. 단 **하드룰**(예: 근거 없는 헤드라인 숫자, 사후 임계 조정)은 티어와 무관하게 항상 blocked다. owner≠reviewer를 지킬 수 없으면 "다른 모델의 적대 다중패스 + 사람 확인"으로 대체하되, 그 사실을 **정직하게 라벨**한다 — 독립성을 확보하지 못했으면서 확보한 척하지 않는다.

### 선례 하나 — 같은 문제를 같은 방향으로 푼 연구

Spark-to-Paper(Qian et al., 2026, arXiv:2608.11924)는 논문 생성을 composable skill로 두고 이 하네스와 독립적으로 같은 설계에 도달했다. 스킬이 파일 아티팩트로만 소통하고, 결정적 게이트와 의미 판단(적대 리뷰)을 분리하며, 실험을 사전등록하고, self-refutation 루프에 상한을 둬 넘으면 실패 리포트를 낸다. ablation에서 검증 스택이 조작 탐지율을 single-pass 14%에서 게이트 69%, 자기검수 81%, 적대 리뷰 92%로 끌어올린다고 보고했다. 남이 같은 구조에 수렴했다는 건 이 스캐폴드의 방향이 옳다는 방증이다. 다만 그들의 검증 스택도 게이트 자체가 공허한지는 보지 않고, 적대 리뷰가 같은 모델의 isolated pass라 precision 74%(오탐 26%)에 머문다. mutation으로 게이트를 검증하는 층과 cross-model·사람으로 올라가는 독립성 사다리가 그 위에 필요한 이유다.
